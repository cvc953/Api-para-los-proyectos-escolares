# Example environment variables for running the API with docker-compose
MYSQL_ROOT_PASSWORD=rootpassword
MYSQL_DATABASE=plataforma_proyectos
MYSQL_USER=appuser
MYSQL_PASSWORD=apppassword

# Optional: override the DATABASE_URL directly
# Recommended: provide a single DATABASE_URL pointing to your MySQL instance.
# Example for a remote MySQL server (replace values):
# DATABASE_URL=mysql+pymysql://appuser:apppassword@mysql-host.example.com:3306/plataforma_proyectos?charset=utf8mb4

# If running locally with the provided compose, these variables will be used by
# the MySQL service and by the API to build the DATABASE_URL automatically.
# DB_HOST and DB_PORT are optional helpers used by the entrypoint wait logic.
# DB_HOST=db
# DB_PORT=3306
